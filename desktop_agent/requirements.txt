# SARA Desktop Control Agent â€” Python dependencies
# Install with the Python 3.11 interpreter, e.g.:
#   "C:\Users\MSI\AppData\Local\Programs\Python\Python311\python.exe" -m pip install -r requirements.txt
# Then install the Playwright browser once:
#   "C:\...\Python311\python.exe" -m playwright install chromium
#
# NOTE: pytesseract also requires the Tesseract OCR engine to be installed
#       on Windows (https://github.com/UB-Mannheim/tesseract/wiki). OCR tools
#       will gracefully report "unavailable" if it is missing; all other
#       capabilities work without it.

fastapi==0.115.6
uvicorn[standard]==0.34.0
pyautogui==0.9.54
playwright==1.49.1
psutil==6.1.1
pywin32==308
pyperclip==1.9.0
pytesseract==0.3.13
Pillow==11.0.0
pycaw==20230407
pygetwindow==0.0.9
Send2Trash==1.8.3
python-multipart==0.0.20
# Best-effort NVIDIA GPU stats; optional import (graceful fallback if absent)
nvidia-ml-py3==7.352.0
cryptography>=40.0.0

# Optional STT dependencies (install one or both for local transcription):
# openai-whisper (pip package: openai-whisper) for Whisper-based local STT
# vosk for offline Kaldi-based STT (requires a Vosk model download)
# soundfile may be required for some audio IO backends
openai-whisper>=2024.0.0
vosk>=0.3.45
soundfile>=0.11.0
google-api-python-client>=2.0.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.2.0
requests>=2.31.0
pypdf>=3.17.0
python-docx>=1.1.0
beautifulsoup4>=4.12.0
opencv-python>=4.8.0
mediapipe==0.10.14
