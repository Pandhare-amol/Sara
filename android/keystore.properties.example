# Copy to keystore.properties locally or use the equivalent SARA_* environment variables.
# Never commit the real file, passwords, or keystore.
storeFile=release/sara-release.keystore
storePassword=REPLACE_LOCALLY
keyAlias=sara
keyPassword=REPLACE_LOCALLY